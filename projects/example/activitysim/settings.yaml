# Example scenario — ActivitySim operational overrides
#
# Small sample for quick testing. Disable shadow pricing for speed.

inherit_settings: True

households_sample_size: 1000
use_shadow_pricing: False
