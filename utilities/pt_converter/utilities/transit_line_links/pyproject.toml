[project]
name = "tm1-transit-line-links"
version = "0.1.0"
description = "Build a directed transit-route link inventory from a CUBE LIN file"
requires-python = ">=3.10"
dependencies = [
  "pandas>=2.0,<3",
  "pyarrow>=12,<22",
]

[tool.uv]
package = false
