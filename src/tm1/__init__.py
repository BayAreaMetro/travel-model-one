from dotenv import load_dotenv

# This loads environment variables from a .env file in the project root, if it exists.
# This is useful for local development, and won't affect production environments where
# environment variables are set differently.
load_dotenv()
