# default-configs/population

PopulationSim configuration — control totals, geographic crosswalks, PUMS seed encoding, and
synthesis settings.

Empty until **phase 4**. Until then the synthetic population comes from the reference run's
`popsyn/` output.

See [`../../MIGRATION_NOTES.md`](../../MIGRATION_NOTES.md).
