---
title: "Test.Rmd"
author: "Lisa Zorn"
date: "Tuesday, August 12, 2014"
output: html_document
---

This R Markdown document is made interactive using Shiny. Unlike the more traditional workflow of creating static reports, you can now create documents that allow your readers to change the assumptions underlying your analysis and see the results immediately. 

To learn more, see [Interactive Documents](http://rmarkdown.rstudio.com/authoring_shiny.html).

```{r test}
TARGET_DIR <- Sys.getenv("TARGET_DIR")
print(TARGET_DIR)
print(nchar(TARGET_DIR))
UNSET_VAR <- Sys.getenv("UNSET_VAR")
print(UNSET_VAR)
print(nchar(UNSET_VAR))

TARGET_DIR <- gsub("\\\\","/",TARGET_DIR)
print(TARGET_DIR)

if (nchar(TARGET_DIR) > 0) {
  opts_knit$set(root.dir=TARGET_DIR)
  setwd(TARGET_DIR)
}
tazData              <- read.table(file="tazData.csv", header=TRUE, sep=",")
print(tazData)
```
